"use strict";
(() => {
    var _a;
    const luffy = 'Luffy';
    const zoro = "Zoro";
    const sanji = `Sanji`;
    console.log(`I'm ${luffy}`);
    console.log(zoro.toUpperCase());
    console.log(((_a = sanji[10]) === null || _a === void 0 ? void 0 : _a.toUpperCase()) || 'Chao mor');
})();
